import React from 'react';
import scss from './styles.module.scss'

const FSA = (events) => {
	const {} = events;
	
	return (<>
		<div className={`${scss.wrap} container`}>

			<p>FSA</p>

		</div>
	</>);
}

export default FSA;